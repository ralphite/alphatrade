# alphatrade — 实验章程

**目标**：作为个人投资者（资金 <$1M，美股，低/中频），用 AI agent 找到**可持续、稳定、有效**的盈利方式。
**重述**：搭一条能快速、便宜、可信地杀死坏主意的验证流水线，最大化单位时间内被严格验证的假设数量。找到能赢的策略是结果，不是计划。

## 方法论基石（不可违背）

1. **LLM 回测带 lookahead bias**：模型训练数据包含历史结局，历史回测虚高且无法量化。
   → backtest 只做粗筛（且尽量用 knowledge cutoff 之后的数据）；**forward paper trading 是主验证手段**。
2. **验证速度 = f(每日独立决策数)**：区分胜率 55% vs 50% 需要几百个样本。
   → 只做每天能产生多个信号的策略形态（事件驱动/横截面）。低频形态验证不动，不立项。
3. **预注册**：每个假设先写 edge 来源、对手盘、预期胜率/盈亏比、kill criteria，然后才允许开测。
   中途不许改 prompt/参数；改了 = 样本计数清零。
4. **成本悲观化**：所有评估含佣金 + 滑点。滑点按 spread 的 50% 或 10bps 取大者；
   小盘股（<$500M 市值或 spread>30bps）翻倍。paper 成交价用信号时刻之后的下一个可得价格，永不用信号时刻价格。
5. **基准**：税后 vs SPY buy-and-hold。短线利得按 short-term capital gains（联邦最高 ~37%）思考。
6. **完整留痕**：每个信号（含未执行的）记录：时间、原始材料、评估 prompt 版本、LLM 推理、决策、成交、退出。
   无留痕 = 未发生。

## 假设流水线关卡

| 关卡 | 内容 | 通过标准 |
|---|---|---|
| 0 登记 | HYPOTHESES.md 写清 edge/对手盘/预期/kill criteria | 写不出对手盘 → 不立项 |
| 1 粗筛 | cutoff 后数据回测或手工复盘 ≥50 个历史事件 | 含成本后仍有正期望的迹象 |
| 2 Forward paper | 全自动信号→模拟成交→记录，2–6 周 | 预注册的 kill criteria |
| 3 小额实盘 | $10k–30k，测真实滑点 | paper edge 保留 ≥60% |
| 4 放大 | $50k→$150k→$500k 分档 | 每档容量衰减可接受 |

当前阶段：**关卡 0–2（全程 paper，不动真钱，不开真实账户）**。

## 风控硬约束（paper 阶段也执行，养习惯）

- 单仓位 ≤ 名义资金（$100k paper）的 3%；组合 gross ≤ 100%。
- 每日新开仓 ≤ 15 个；单日 paper 回撤 >3% → 当日停止开新仓。
- 只交易：美股 common stock，价格 >$3，20 日均成交额 >$2M。
- 现阶段 long-only（做空留待有 borrow 数据后）；用 SPY 对照衡量超额。

## 基础设施

- 数据：SEC EDGAR（免费实时 8-K，User-Agent 合规声明）；yfinance 价格（延迟容忍，因为持有期为小时~天）；Stooq 日线备份。
- LLM 评估：Claude（本 agent 及 subagents），评估 prompt 固化在 prompts/ 下并版本化。
- 账本：本地 JSONL/CSV 自建 paper ledger（无券商依赖）。
- 编排：Claude Code loop 模式，按市场时间自主唤醒。

## 角色分工

- Agent（Claude）：全部研究、代码、评估、执行、报告。
- 用户：读日报，对"是否进入下一关卡/是否杀死假设"有最终否决权。
