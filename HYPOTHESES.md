# 假设登记簿

状态：`draft` → `screening`(关卡1) → `forward`(关卡2) → `live-small`(关卡3) → `scaled` / `killed`

---

## H0 — 管线校准弹（隔夜持有效应）status: screening

**性质**：不是 alpha 假设。目的是用一个已知的、可用传统日线数据严格回测的简单规则，端到端跑通「信号→成交模型→账本→评估报告」，验证管线本身无 bug，并建立成本模型的手感。
**规则**：收盘买入 QQQ，次日开盘卖出（close-to-open）。已知文献：美股近几十年隔夜收益占了绝大部分累计收益，日内收益约为零/负。
**用途**：回测 2020–2026，对照「开盘买收盘卖」与 buy-and-hold。数字对得上文献 → 管线可信。不进入 forward 阶段，不占用验证管线。

## H1 — 8-K 事件驱动（LLM 文本理解）status: draft → 今日进入 screening + forward

**Edge 假设**：小/中盘股的 8-K（重大事件公告）在发布后的数小时内价格反应不完全。LLM 能以人类不可能的吞吐量读完全部 8-K 全文，识别其中方向性明确、但市场尚未充分定价的事件（underreaction / PEAD 文献族）。
**对手盘（谁输钱给我们）**：(a) 读得太慢、第二天才从新闻摘要看到的人类投资者；(b) 只做关键词/分类器、读不懂复杂措辞的传统算法；(c) 因容量太小而不覆盖小盘事件的机构。
**为什么现在存在**：LLM 级阅读理解 2023 年后才可用；小盘 8-K 的覆盖仍然稀疏。
**信号定义 v1**：
- 输入池：EDGAR 实时 8-K feed，过滤 item ∈ {1.01, 1.02, 2.01, 2.02, 4.02, 5.02, 7.01, 8.01}，标的满足风控约束（>$3, ADV>$2M），排除 mega-cap（>$50B，那里定价快）。
- LLM 评估（prompts/eval_8k_v1.md）输出：direction ∈ {long, skip}（v1 long-only）、conviction ∈ {1,2,3}、一句话论点、预期催化持有期。
- 仅 conviction=3 且 direction=long → 进入 red-team 关：一个独立 devil's-advocate agent 对每个 conv3 信号写反驳（不知道原评估者的结论理由，只看原始材料），主 agent 与其 argue 后仲裁。**存活 → 执行开仓 2%；被驳倒 → 降级为影子信号（conviction 记为 "3-vetoed"，照常记录跟踪）**。
- conviction=2 → 记录不交易（影子信号，用于事后检验阈值）。
- 退出：持有至第 2 个交易日收盘（T+2 close），或 -4% 止损，先到者为准。
- （2026-07-10 Day 0 开测前定稿，来自用户建议：red-team 关是执行层质量闸，eval_8k_v1 本身不变。vetoed 信号必须跟踪，用于检验红队是否在帮忙还是在帮倒忙。）
**预期（预注册）**：信号频率 3–10 个/交易日（含影子）；执行信号胜率 52–58%；平均单笔净收益 > +15bps（含成本）；vs 同期 SPY 超额为正。
**Kill criteria（预注册）**：
- 累计 ≥150 个执行信号后：excess profit factor <1.1，或平均单笔超额收益（excess = ret_net − 同窗口 SPY 收益）≤0 → **杀死或大改（大改=清零重测）**。
- 运行 4 周后执行信号 <40 个 → 信号太稀，改造过滤器（计数清零）。
- 影子信号（conviction≤2）表现显著好于执行信号 → 阈值定义有问题，改造（计数清零）。
**推进标准（2026-07-10 红队审查后收紧，先于任何正式样本）**：进入关卡 3（真钱）不是"没被杀"，而是需要证据：n≥150 且平均 excess 的 t-stat ≥ 2（或 bootstrap 95% CI 下界 >0）。t∈(0,2) = 继续积累样本。**所有评估指标一律用 excess（对 SPY），绝对收益只作参考**——long-only 策略在牛市里的绝对正收益不是 edge。
**样本量诚实声明**：若真实 edge 只有 +15bps/笔（单笔 std ~4%），任何现实样本量都无法分辨——本假设赌的是 conv3 精选后 edge 达到 +50~100bps/笔 量级，这在 150–300 样本上可检验（t≈1.5–3）。若跑完 300 样本 excess t<1 → 视同 kill。"稳定盈利"的确认仍需 6–12 个月穿越不同市场环境。
**Day-0（2026-07-10）声明**：当日 46 条评估受正文抽取 bug（XBRL 封面）与 accepted 时间缺失影响，全部标记 warmup，**不计入正式样本**。正式计数从管线修复完成后的下一批信号开始。

## H1 v1 判定：KILLED（2026-07-10，关卡 1 不通过）

**证据**：两个独立历史窗口（2026-06-09..11 n=194；06-23..25 n=208，评估模型 cutoff 后、无 lookahead、含悲观成本）+ Day-0 forward 案例（WDFC）三重印证：
- conv3 池内差分：窗1 -52bps、窗2 -517bps（8/8 中 7 个负，窗2 四个全亏）；conv2 池内差分：-19 / -274bps。
- skip 对照池方向随窗口翻转（-131 vs +93bps vs SPY）→ 一切评估必须池内差分。
**机制结论（本实验最重要产出）**：LLM 方向判断本身与市场一致（conv3 标的相对事件前确实上涨），但 alpha 在「事件发布 → 次日开盘 gap」内释放完毕；次日开盘入场吃到的是 gap 后 fade（过度反应回归），不是 underreaction drift。死因是**入场时点结构**，不是文本理解能力。

## H1b — gap-fade 反向（draft，登记 2026-07-10）

**Edge 假设**：明显利好事件次日开盘 gap 过度，开盘做空 conv3 类标的、T+2 回补。历史差分暗示毛 edge ~+290bps/2d，但需扣：借券成本（小盘 hard-to-borrow 年化 5-50%+）、做空滑点、gap 幅度筛选。对手盘：开盘追高的动量散户。
**先决**：历史模拟改为 short 方向重算 + borrow 成本模型；两窗口差分幅度不稳定（-52 vs -517），需要第三窗口。**未通过前不进入 forward。**

## H1c — 盘后即时入场（draft，登记 2026-07-10，优先）

**Edge 假设**：8-K 高峰在 16:00–18:00 ET 盘后。若在盘后时段（公告后 15–60 分钟）以盘后价格入场，抢在次日开盘 gap 完成之前，可捕获 gap 本身。个人投资者可无障碍参与盘后交易；机构算法在小盘盘后流动性稀薄处覆盖不全。对手盘：只在正常时段交易的参与者。
**先决（历史可测！）**：yfinance prepost 分钟数据 → 对两窗口全部 conv3/conv2 重算「盘后 T+30min 入场 vs 次日开盘入场」的差异。若盘后入场差分显著为正 → 立项进 forward。风险：盘后 spread 极宽（滑点模型要另建，按 100-300bps 计）、成交量薄导致 paper 成交假设过于乐观。
**周末任务：先跑这个历史模拟。**

## 候选池（未立项）

- H2 候选：earnings call transcript 语气/guidance 变化 vs 盘后价格反应（数据源待解决）。
- H3 候选：盘前新闻聚类的横截面 open-drive（需要新闻 API，$）。
- H4 候选：传统小容量异象扫描（Agent 作为研发加速器，纯 quant 战场）。
